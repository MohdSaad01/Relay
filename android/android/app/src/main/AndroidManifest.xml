<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Foreground transfer handling (@supersami/rn-foreground-service): keeps an
         active upload/download running when the app is backgrounded, per
         docs/11_File_Transfer.md and the approved M15 design's "Background/
         foreground lifecycle" section. FOREGROUND_SERVICE_DATA_SYNC + a matching
         android:foregroundServiceType on the <service> tags below are mandatory
         on Android 14+ (targetSdkVersion 36 here) for a network-transfer service. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- android:usesCleartextTraffic below is a React Native Gradle Plugin
         manifest placeholder (resolves to "true" on debug, "false" on
         release — confirmed via AgpConfiguratorUtils.kt, P37). Since minSdk
         is 26 (>= 24), android:networkSecurityConfig always takes
         precedence over it (see res/xml/network_security_config.xml for
         why release-build cleartext HTTP must be explicitly permitted), so
         this attribute has no real effect; left in place only because
         removing it is out of P40's scope and it is otherwise harmless. -->
    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:networkSecurityConfig="@xml/network_security_config"
      android:supportsRtl="true">

      <meta-data
        android:name="com.supersami.foregroundservice.notification_channel_name"
        android:value="Relay Transfers" />
      <meta-data
        android:name="com.supersami.foregroundservice.notification_channel_description"
        android:value="Shows progress while a file transfer is active." />
      <meta-data
        android:name="com.supersami.foregroundservice.notification_color"
        android:resource="@color/blue" />

      <service
        android:name="com.supersami.foregroundservice.ForegroundService"
        android:foregroundServiceType="dataSync"
        android:exported="false" />
      <service
        android:name="com.supersami.foregroundservice.ForegroundServiceTask"
        android:foregroundServiceType="dataSync"
        android:exported="false" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
